CREATE TABLE sensor_data (
                             id BIGINT AUTO_INCREMENT PRIMARY KEY,
                             uv_index DOUBLE,
                             air_quality INT,
                             observed_at TIMESTAMP
);